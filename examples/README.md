# Example input layout

Copy `samplesheet.tsv` into a study directory, create the shown `data/` and `reads/` paths, and replace the placeholder filenames with your matched assemblies and paired reads. Relative paths are resolved from the samplesheet directory.

No fabricated allele FASTA is included: use the authoritative allele set for the exact locus and scheme being analyzed.

